#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main (void)
{
    long n = get_long("Card's number: ");
    string x = n;
    int length = strlen(x);
    for (i = (length - 1); i == 0; i--)
    {
        int d = x(i);
        string f(i) = 2 * d;
    }



}

